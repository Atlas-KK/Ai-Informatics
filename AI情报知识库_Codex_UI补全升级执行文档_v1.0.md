# AI 产品经理情报知识库 Codex UI 补全升级执行文档

> 文档版本：v1.0-UI  
> 编制日期：2026-09-06  
> 目标范围：现有 Phase 7 本地网页对齐 PRD v3.0 与 Figma v2  
> 准备度：READY WITH TECHNICAL ASSUMPTIONS  
> 授权状态：仅生成执行规划；尚未授权任何实现阶段

## 1. 文档信息与权威顺序

### 1.1 权威资料

1. 当前用户的明确指令；
2. `D:\AI_informatics\Ai情报搜集系统\AI情报知识库_PRD_工作稿.md`，v3.0；
3. `D:\AI_informatics\Ai情报搜集系统\AI情报知识库_Codex开发执行文档_v1.1.md`，文档内版本 v1.1-AC；
4. `D:\AI_informatics\Ai情报搜集系统\ai-intel-kb\README_FOR_AI.md`，最近核验 2026-09-05；
5. Figma 文件 `PjXzkYaVIX4BxV26eW1Tk6` 的页面 `v2 · PRD 完整交互原型`（page `19:2`）；
6. 当前代码、数据库迁移、API 契约和测试；
7. 原 Figma UI 验收报告及其他历史资料。

冲突处理：

- PRD 定义产品行为、分类、阈值、权限和验收语义；
- Figma v2 定义拟确认的布局、信息层级、控件位置和状态表达；
- 当前代码定义受保护的既有行为，不得以“代码已经如此”为理由否定 PRD；
- Figma 演示数据、日期、网址、状态快照和跳转方式不得覆盖 PRD 或真实 API；
- 本文档是 v1.1-AC 的 UI 专项补充，不替代完整系统执行文档，不修改 PRD。

### 1.2 使用规则

- Codex 一次只执行用户明确批准的一个 UI Phase。
- 每阶段开始前必须报告覆盖的 FR/AC、拟改文件、测试、冲突和停止条件。
- 每阶段结束后运行真实质量命令，给出实际结果并停止；不得自动进入下一阶段。
- 不得把 121 个 Figma 状态画面实现为 121 个独立路由。
- 不得硬编码 Figma 中的 `2026-09-04`、`EVT-032`、`example.com`、演示分数或错误日志。
- 不得在前端复制评分、去重、版本、任务锁、归档或持久化规则；前端只展示服务端权威结果。
- 不得新增依赖、迁移、真实外部适配、凭证、系统任务、提交、推送或部署，除非用户另行授权。
- 全部外部正文按纯文本或经过既有安全清洗的内容展示；禁止 `dangerouslySetInnerHTML`。

## 2. 就绪审计

### 2.1 需求与批准状态

- **已确认**：既有 v1.1-AC 执行文档明确记录 PRD v3.0 已确认。
- **已确认**：Harness 将 PRD v3.0、v1.1-AC、代码测试列为权威链，并记载 Phase 1～7 已实现。
- **已确认**：用户本轮要求基于新 Figma UI 生成面向 Codex 的 UI 补全升级文档。
- **待确认 UI-DEC-01**：情报详情最终采用 Figma v2 的主内容独立页，还是保留当前 720px Drawer。本文默认推荐独立页，但 UI-0 必须由用户确认。
- **待确认 UI-DEC-02**：Figma 的“原型导览/状态验收”只用于设计验收，默认不进入生产导航。
- **待确认 UI-DEC-03**：Figma 结果状态可由页面内 Alert、Result、Modal 或局部状态表达；只要信息和后续操作一致，不要求逐帧复刻。
- **待确认 UI-DEC-04**：迁移 `0009_phase7_local_web.py` 和当前前端/E2E fixture 使用 `3/7/10`，而 PRD/Figma 默认口径为 `10/20/20`。新建数据库必须改为 PRD 默认值；既有数据库可能包含用户配置，未经确认不得自动覆盖。

### 2.2 当前仓库事实

| 项目 | 当前事实 | 影响 |
|---|---|---|
| 仓库 | Git 根目录为 `D:\AI_informatics\Ai情报搜集系统`，分支 `main`；项目仍整体未跟踪 | 无法依赖 Git diff 区分用户既有内容；编辑前后必须逐文件核对 |
| 前端 | React 19.2.8、TypeScript 6.0.3、Vite 8.2.2、Ant Design 6.6.2、ECharts 6.1.0 | 优先复用现有库，不新增路由或状态管理依赖 |
| 页面组织 | `web/src/App.tsx` 集中应用壳、8 个主页面、详情 Drawer 和操作逻辑，约千行 | 需要按 feature 拆分，避免继续扩大单文件 |
| 导航 | 根组件用字符串 `page` 切换；无路由依赖 | 独立详情页可先用类型化 `ViewState` 实现；是否需要可复制 URL 另行确认 |
| API | 已有 dashboard、topics、archive/detail、note/metadata/trash/restore/delete、search、runs/retry、settings/config-status、feedback/calibration、expansion/favorite | 多数业务能力可复用；部分 Figma 展示缺少读取契约 |
| 测试 | 1 个基础 Vitest；Phase 7 mock E2E 覆盖 9 个写操作；Phase 8 有真实 FastAPI/SQLite 浏览器 E2E 和 1 万条性能证据 | UI 改造必须扩充组件状态与主要流程测试，同时保留既有 E2E |
| 数据边界 | SQLite/Archive 为权威；用户元数据独立；Raw/EventVersion/Evidence/Score 只追加 | UI 不得直接改写系统档案或模拟持久化成功 |
| 外部能力 | 真实来源、LLM、Embedding、扩展搜索、飞书未获授权 | UI 使用真实配置状态或 Fake；不能声称外部服务已接通 |
| 现有 UI 文案 | `App.tsx` 仍使用“主题档案”和“延伸” | 与 PRD/Figma 的“专题档案”和“扩展”冲突，UI-1 必须修正 |
| 分层默认值 | 迁移、Vitest 和 mock E2E 使用 `3/7/10` | 与 PRD/Figma 默认 `10/20/20` 不一致；按 UI-DEC-04 处理新库和既有库 |

### 2.3 2026-09-06 实际质量基线

在未修改应用代码的前提下执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/quality.ps1 -Scope full
```

结果：退出码 0。Secret scan 通过；Ruff、格式检查、mypy（71 个源文件）通过；pytest 为 102 passed、2 条第三方弃用警告；Vitest 为 1 passed；前端 build 通过；Phase 7 浏览器 E2E 验证 9 个写操作；真实 FastAPI/SQLite 的 1 万条浏览器检查记录冷启动约 1.08 秒、筛选约 0.12 秒。该结果只是变更前基线，不代表 UI 升级已经实现或验收通过。

### 2.4 Figma v2 核验事实

- 文件：[Figma v2 功能导览](https://www.figma.com/design/PjXzkYaVIX4BxV26eW1Tk6?node-id=23-2)。
- 主流程：[今日情报](https://www.figma.com/design/PjXzkYaVIX4BxV26eW1Tk6?node-id=25-170)。
- 状态索引：[状态验收](https://www.figma.com/design/PjXzkYaVIX4BxV26eW1Tk6?node-id=42-1869)。
- 设计复核记录为 121 个页面/状态画面、1,514 个热点、0 个断链、3 个基础组件、26 个变量、5 个文本样式。
- 本轮再次抽查了历史检索 `25:313`、情报详情 `23:50`、运行记录 `28:540` 和评分校准 `27:541`。
- 这些画面是布局与状态参考，不是数据库状态机或 API 契约。

### 2.5 准备度结论

**READY WITH TECHNICAL ASSUMPTIONS。**

PRD、代码、API、测试和 Figma 均足以形成执行计划。Harness 中“Figma 不可作为实现基线”的结论对应补全前原型；v2 已关闭画面和连线缺口，但尚未得到产品方对 UI 形式的最终确认，因此不能静默删除这条门禁。UI-DEC-01～04 必须在 Phase UI-0 确认。其余架构拆分、服务端筛选、类型收敛和测试增强属于可逆技术建议。

## 3. 目标与非目标

### 3.1 本次目标

1. 将现有本地网页的信息架构、视觉层级和状态表达对齐 Figma v2。
2. 让“来源配置 → 采集观测 → 情报浏览/精读 → 扩展搜索/选题 → 反馈校准 → 删除恢复”在真实 API 下形成可操作闭环。
3. 将单体 `App.tsx` 拆为可维护的 feature、共享组件、类型和服务层。
4. 为加载、空数据、数据不完整、失败、重试、禁用、成功和危险确认建立统一状态模式。
5. 保持本地单用户、只读档案、记录级操作、Fake 边界、安全展示和 1 万条性能基线。

### 3.2 明确非目标

- 不实现真实网络采集、真实 LLM、Embedding、扩展搜索或飞书适配。
- 不创建系统计划任务，不开放公网地址，不引入多用户与权限体系。
- 不修改 PRD 的四专题、70 分阈值、50 条上限或 10/20/20 分层。
- 不将 Obsidian 变为运行前置，不改变 Raw/Archive/Vault View 单向边界。
- 不做移动端编辑后台，不新增复杂知识图谱，不自动发布内容。
- 不为逐像素复刻 Figma 而牺牲可访问性、真实数据状态或既有性能。
- 不把原型导览、状态验收和演示样例作为生产功能。

## 4. Figma 到生产 UI 的解释契约

### 4.1 生产信息架构

| 生产模块 | Figma 参考 | 实现形态 |
|---|---|---|
| 今日情报 | `today`、`dashboard`、空/不完整状态 | 主页面；统计卡、四类图表、四专题入口、分层列表 |
| 专题档案 | `topics`、各专题列表、`topics_reordered` | 主页面 + 专题视图；名称统一使用“专题” |
| 历史检索 | `history`、`filters`、结果/加载/空/失败/降级 | 主页面；真实查询参数与结果状态 |
| 情报详情 | `detail`、`evidence`、`versions`、`ideas` | 一个详情工作区 + 4 个标签，不拆成 4 个业务对象 |
| 来源管理 | 来源、专家、GitHub、视频与操作结果 | 主页面 + 标签 + 表单/确认框 |
| 质量反馈 | 反馈、校准、版本、回退、重评分 | 主页面 + 分步骤工作台 |
| 运行记录 | 运行列表、详情、超时、恢复、推送分段 | 主页面 + 详情面板 |
| 系统设置 | 调度、评分分层、顺序、服务状态 | 主页面 + 标签；评分权重只能经校准流程变更 |
| 回收站 | 列表、恢复、永久删除 | 主页面 + 危险确认 |

### 4.2 文案和业务口径

- 导航统一为“专题档案”，不得继续使用当前代码中的“主题档案”。
- `EXTENDED` 的中文标签统一为“扩展”，不得使用当前代码中的“延伸”。
- 五维评分展示为“来源权威性、及时性、传播范围、信息密度、创新性”，英文键只用于协议。
- 观点覆盖比例明确标注为“支持来源数 / 有效来源总数”，不得称置信度。
- 系统档案字段只读；个人笔记、收藏、置顶、已读独立显示和提交。
- 缺少发布时间显示“发布时间未知”并展示首次发现时间；不得伪造发布日期。
- 评分权重调整必须进入校准建议和确认流程。设置页可以显示当前权重与版本，但不得提供绕过确认的直接保存。

### 4.3 状态收敛原则

Figma 的成功、失败、取消、空数据、处理中画面应收敛为以下共享状态：

- `AsyncState = idle | loading | success | empty | partial | error`；
- 页面级：`PageState`、`ErrorState`、`EmptyState`；
- 操作级：按钮 loading、局部 Alert、message；
- 危险操作：Modal/Popconfirm + 明确对象、影响、取消与确认；
- 后台运行：已完成档案继续可用，处理中记录禁用详情与用户操作；
- 表单：字段级校验、服务端错误、草稿保留、提交成功后的权威数据刷新。

## 5. 现状差距与最小改动

| 需求区域 | 当前能力 | 差距 | 最小改动 | 分类 |
|---|---|---|---|---|
| 应用壳 | 8 个菜单、响应式 Sider | 文案不一致；页面和业务状态集中在根组件 | 提取 Shell、类型化 ViewState、导航配置和设计 token | 既有行为适配 |
| 今日首页 | 状态、统计、4 图表、列表 | 四专题入口弱；空日报与部分数据信息不足 | 增加专题快捷入口和明确空/部分态 | 增量 UI |
| 专题 | 按设置顺序分组列表 | 一次加载最多 200 条；缺二级标签和独立专题态 | 服务端专题查询/数量；专题页组件 | API + UI |
| 检索 | 关键词/语义 API + 前端筛选排序 | 先取最多 200 条再前端筛选，不适合 1 万条；错误仅 message | 扩展服务端筛选、排序、分页/上限；完整状态区 | API + UI |
| 详情 | 720px Drawer；正文、版本、来源、选题、扩展、笔记 | 信息层级与 Figma 不一致；来源名/类型/覆盖比例不足；只读与可编辑区域不够分明 | 按 UI-DEC-01 实现详情工作区；扩展 detail DTO | API + UI |
| 用户元数据 | 收藏、置顶、已读、笔记、剔除、恢复、永久删除已可调用 | 缺统一 pending/error；笔记失败草稿和操作后可见状态不足 | 统一 mutation 状态、刷新策略与回滚提示 | UI |
| 来源 | 来源/专家 CRUD、暂停、恢复、逻辑删除 | 缺最近采集/最后结果；无 GitHub 规则页；视频边界弱 | 只读状态字段 + GitHub 规则最小契约和 UI | API + UI；可能迁移 |
| 质量校准 | 提案、确认、拒绝、手填版本回退 | 缺配置版本列表、操作历史、重评分范围与结果 | 添加读取历史和显式重评分契约；版本选择不得自由猜测 | API + UI |
| 运行 | 汇总表和失败阶段重试 | 缺来源/阶段/时间/原因/次数详情、超时、遗留锁、飞书分段 | 增加 run detail DTO/接口与详情面板 | API + UI |
| 设置 | 时间、阈值、分层、顺序、默认排序、服务布尔状态 | 缺人类可读服务状态、评分版本/权重展示和校验说明 | Tabs 分组；当前评分配置只读；缺失项只显示名称 | API + UI |
| 分层配置 | 数据库初始值和测试 fixture 为 3/7/10；Repository 只校验非负 | PRD/Figma 默认 10/20/20；未限制总量或明确值的“数量/累计边界”语义 | 新库改默认；既有配置按 UI-DEC-04；明确 DTO 为各层数量并校验总量不超过 50 | 配置契约冲突 |
| 选题 | 详情读取已有选题 | 非必读手动生成缺入口/API；缺支撑来源 | 增加显式生成端口和 DTO，Fake 默认 | API + UI |
| 扩展搜索 | 搜索、失败状态、收藏入库、后端去重已存在 | UI 忽略收藏返回的 `deduplicated/event_id`；缺确认和结果区分 | 使用真实返回值展示“新建/关联已有事件” | UI |
| 测试 | 后端强、前端薄；已有 2 条浏览器链路 | 组件状态、可访问性和 Figma 关键流程覆盖不足 | 增加 feature 测试、契约 fixture 和 6 条 UI 流程 E2E | 测试 |

## 6. 目标前端架构

以下目录为**技术建议**，以职责为准，不要求机械采用名称：

```text
web/src/
├─ app/
│  ├─ AppShell.tsx
│  ├─ navigation.ts
│  └─ view-state.ts
├─ components/
│  ├─ AsyncStateView.tsx
│  ├─ ConfirmAction.tsx
│  ├─ IntelList.tsx
│  ├─ ScoreBreakdown.tsx
│  ├─ StatusBadge.tsx
│  └─ PageHeader.tsx
├─ features/
│  ├─ dashboard/
│  ├─ topics/
│  ├─ search/
│  ├─ intel-detail/
│  ├─ sources/
│  ├─ quality/
│  ├─ runs/
│  ├─ settings/
│  └─ trash/
├─ hooks/
│  ├─ useAsyncResource.ts
│  └─ useAsyncAction.ts
├─ services/
│  ├─ api.ts
│  └─ contracts.ts
├─ styles/
│  ├─ tokens.css
│  └─ app.css
├─ test/
│  └─ fixtures/
└─ main.tsx
```

### 6.1 所有权

| 所有者 | 职责 | 禁止事项 |
|---|---|---|
| `app` | 导航、当前视图、返回上下文、页面装配 | 存放领域规则或复制 API 查询 |
| `features/*` | 单业务页面的数据请求、局部状态和操作编排 | 跨 feature 直接改他人状态 |
| `components` | 无业务副作用的展示和通用交互 | 直接请求 API、硬编码 PRD 阈值 |
| `services` | HTTP、错误规范化、DTO 类型 | 把响应伪装为成功、吞掉错误 |
| `hooks` | loading/error/cancel/retry 等通用异步生命周期 | 保存领域权威状态 |
| FastAPI/Repository | 筛选、分页、详情聚合、运行/评分读取契约 | 为迎合画面改变领域语义 |

### 6.2 状态与同步

- **服务端状态**：档案、评分、来源、运行、配置、反馈与回收站以 API 为唯一权威。
- **UI 状态**：当前页面、活动标签、筛选草稿、Modal 开关、请求 pending/error。
- 操作成功后重新读取最小相关资源；不得仅修改本地数组假装持久化。
- 列表返回详情时保留筛选和滚动上下文；刷新后的元数据以服务端结果为准。
- 同一资源并发提交时禁用重复按钮；不得产生双重剔除、重复收藏归档或重复校准确认。
- AbortController 仅用于取消过时读取；已经发出的写操作不能因组件卸载被视为失败或回滚。

### 6.3 API 契约增补

Codex 在实现前必须先核对现有 schema/table；能通过查询投影完成的不得先做迁移。

| 契约 | 建议 | 边界 |
|---|---|---|
| `GET /api/archive` | 增加日期、来源、标签、最低分、排序、分页/总数 | 排序规则仍由后端统一；不加载全部数据到前端 |
| `GET /api/search` | 接收同一组筛选/排序参数并返回模式、降级原因、总数 | 语义不可用时明确 KEYWORD 降级 |
| `GET /api/archive/{event_id}` | evidence 增加来源名/类型；返回观点分组与覆盖比例；发布时间允许 unknown 表达 | 覆盖比例由后端计算；前端不推断 |
| `GET /api/runs/{run_id}` | 返回 work items、失败阶段、时间、原因、次数、可重试范围及 delivery segments | 不暴露密钥、Cookie、认证头或原始敏感正文 |
| `GET /api/calibration/configs` | 返回版本、active、权重、应用/回退时间 | 仅供选择真实历史版本 |
| `POST /api/calibration/rescore` | 明确范围、配置版本、confirmed，返回新增记录统计 | 保留旧分；无确认不得执行 |
| `GET/PUT /api/github-rules` | 日榜、周榜、7日 Star、AI 相关性、项目白名单 | 没有足够 Star 基线时返回 unknown |
| `POST /api/archive/{event_id}/topic-idea` | 非必读情报手动生成；返回 pending/success/failure | 真实 LLM 未授权时明确 503/未配置，不伪造 |
| `POST /api/extension-results/{id}/favorite` | 复用现有 `event_id/deduplicated` 返回值 | UI 必须区分新档案和关联已有事件 |

任何迁移都必须在阶段开始前列出表/列、兼容性、空库升级与既有数据升级测试，并获得该阶段授权。

## 7. 分阶段执行

### Phase UI-0：只读差距复核与界面决策

| 字段 | 内容 |
|---|---|
| 目标 | 冻结本次 UI 实现基线，确认 UI-DEC-01～04 |
| 覆盖 | F10；所有 AC 的追踪基线 |
| 前置 | 读取四份权威文件、Figma v2 和当前代码 |
| 文件 | 不修改任何文件 |
| 任务 | 检查工作树；输出现状截图；核对 Figma 页面映射；确认详情形态、原型辅助页是否生产化、状态表达自由度；决定既有 3/7/10 配置的保留/迁移策略；列出必要 API 缺口 |
| 禁止 | 写代码、改 PRD、装依赖、改迁移、生成生产数据 |
| 验证 | 记录 `git status --short`；运行或引用最新 `quality.ps1 -Scope full` 基线 |
| 证据 | 决策表、差距表、计划文件清单、基线命令结果 |
| 停止门禁 | 用户确认 UI-DEC-01～04 和 Phase UI-1 范围 |

### Phase UI-1：设计基础、应用壳与可测试拆分

| 字段 | 内容 |
|---|---|
| 目标 | 不改变业务行为地拆分 `App.tsx`，建立 Figma token、导航和状态组件基础 |
| 覆盖 | F10；AC-D01、D03、D04、D11；AC-I01 |
| 前置 | UI-0 已确认 |
| 计划文件 | 修改 `web/src/App.tsx`、`App.css`；新增 `app/*`、`components/*`、`styles/*` 和测试 |
| 任务 | 提取 Shell、导航、PageHeader、StatusBadge、AsyncStateView、ConfirmAction、IntelList；统一“专题档案/扩展”文案和五维标签；建立类型化 ViewState |
| 禁止 | 改 API、改领域规则、引入路由/状态管理依赖、改变用户数据语义 |
| 测试 | 组件渲染、键盘导航、active menu、空/失败/部分态；保留现有 App 测试 |
| 验收证据 | 1440×1000 应用壳截图；组件测试；无硬编码演示数据 |
| 质量门禁 | `powershell -File scripts/quality.ps1 -Scope frontend` |
| 停止门禁 | 用户确认应用壳、字体/颜色、导航和通用状态 |

### Phase UI-2：读取契约、今日、专题与历史检索

| 字段 | 内容 |
|---|---|
| 目标 | 用服务端筛选与可观察状态完成三类浏览入口 |
| 覆盖 | F5、F6、F10、F11；AC-F14～21、F25；AC-D01～05、D13～14 |
| 前置 | UI-1 通过；筛选 API 方案确认 |
| 计划文件 | `web/src/features/dashboard/*`、`topics/*`、`search/*`、`services/contracts.ts`；必要时修改 `api/app.py`、`api/schemas.py`、`phase7_repository.py` 及 Phase 7 测试 |
| 任务 | 四专题入口；三级标识；四类图表；空/部分态；专题数量/标签；服务端筛选、排序、分页；loading/empty/error/degraded |
| 禁止 | 前端自行计算达标、分层或覆盖 1 万条的全量数据；改变阈值/排序语义 |
| 测试 | 过滤组合、清空、排序、无结果、错误、语义降级、空日报、数据不完整、1 万条性能回归 |
| 验收证据 | 对应 Figma 截图；API 契约测试；浏览器检索流程 |
| 质量门禁 | 定向 Vitest + `pytest tests/phase7 -q` + `quality.ps1 -Scope full` |
| 停止门禁 | 用户确认首页密度、专题入口与检索交互 |

### Phase UI-3：情报详情、版本、证据与用户元数据

| 字段 | 内容 |
|---|---|
| 目标 | 完成可精读、可追溯、系统档案只读的详情工作区 |
| 覆盖 | F3、F4、F6、F8、F10；AC-F05、F08～12、F16、F22；AC-D06～11；AC-I01～08；AC-E14 |
| 前置 | UI-DEC-01 已确认；详情 DTO 方案确认 |
| 计划文件 | `features/intel-detail/*`、`ScoreBreakdown.tsx`、类型/服务；必要时扩展 detail repository/API/tests |
| 任务 | 正文/来源/版本/选题四标签；英文原文、译文、摘要；来源名/链接/发布时间/观点/覆盖比例；首次/历史/最新版本切换；评分依据/版本；笔记及收藏/置顶/已读；剔除原因 |
| 禁止 | 编辑系统字段；前端计算覆盖比例；执行外部 HTML；切换版本时覆盖主事件笔记 |
| 测试 | 多版本、多来源、发布时间未知、来源不可访问、只读、笔记失败保留、刷新持久化、处理中禁用 |
| 验收证据 | 详情四标签截图；真实 API 响应；Mutation 前后数据证据 |
| 质量门禁 | 定向前后端测试 + `quality.ps1 -Scope full` |
| 停止门禁 | 用户确认详情信息层级、操作区和危险入口 |

### Phase UI-4：来源、专家、GitHub 规则与系统设置

| 字段 | 内容 |
|---|---|
| 目标 | 完成来源运营和设置的可验证 CRUD/配置流程 |
| 覆盖 | F1、F2、F5、F6、F10；AC-F01～07、F13～15；AC-D14；AC-I14；AC-E02、E13 |
| 前置 | UI-3 通过；GitHub 规则和状态字段存储方案确认 |
| 计划文件 | `features/sources/*`、`settings/*`；必要时 API/schema/repository/migration/tests |
| 任务 | 来源表、表单校验、暂停/恢复/逻辑删除；最近采集和失败状态；专家管理；GitHub 规则；视频边界；设置 Tabs；当前评分权重只读；专题顺序和服务状态；新库默认分层修正为 10/20/20，既有配置按 UI-DEC-04 处理 |
| 禁止 | 显示/保存完整密钥；直接编辑评分权重；删除历史档案；未授权真实连接测试 |
| 测试 | 新增/编辑/校验、逻辑删除保留历史、暂停恢复、截断下次生效、专题顺序、缺配置项不泄密 |
| 验收证据 | 来源各状态、设置四标签截图；API/迁移测试 |
| 质量门禁 | 定向测试 + 空库迁移/既有库升级（若有迁移）+ `quality.ps1 -Scope full` |
| 停止门禁 | 用户确认表单密度、危险文案和设置分组 |

### Phase UI-5：质量反馈、校准版本、回退与重评分

| 字段 | 内容 |
|---|---|
| 目标 | 实现反馈不自动改规则、明确确认后版本化生效的完整工作台 |
| 覆盖 | F7、F10；AC-I06、I09～13；AC-M01 |
| 前置 | 版本历史和重评分 API 获批 |
| 计划文件 | `features/quality/*`、contracts；必要时 calibration API/repository/tests |
| 任务 | 反馈列表/详情；样本量、不确定性、建议权重、影响范围；确认/拒绝；版本历史；回退确认；重评分范围与结果 |
| 禁止 | 提交反馈即改权重；手填不存在的版本；静默覆盖旧评分；样本不足仍生成新配置 |
| 测试 | 未发起校准、样本不足、确认、拒绝、回退、重评分新增记录 |
| 验收证据 | 校准前后 active version、历史记录、新旧评分并存 |
| 质量门禁 | Phase 5/7 定向后端测试 + 前端测试 + `quality.ps1 -Scope full` |
| 停止门禁 | 用户确认建议展示、确认风险和版本历史 |

### Phase UI-6：运行详情、阶段重试与飞书分段

| 字段 | 内容 |
|---|---|
| 目标 | 让失败、超时、重复启动、恢复和分段补发在网页可观察可操作 |
| 覆盖 | F2、F9、F10；AC-D12；AC-E01～11；AC-M02～04 |
| 前置 | run detail/delivery DTO 获批；真实发送仍未授权 |
| 计划文件 | `features/runs/*`、contracts；必要时 runs API/repository/tests |
| 任务 | 运行列表/详情；失败来源、阶段、时间、原因、次数；只重试失败阶段及后续；超时与待补跑；锁恢复；重复触发；飞书成功/失败分段与仅补失败分段 |
| 禁止 | 前端决定可重试阶段；自动无限重试；重发成功分段；真实飞书发送 |
| 测试 | 局部失败、全部失败、超时、重复触发、遗留锁、分段幂等、日志脱敏 |
| 验收证据 | 状态与后端 work item/delivery segment 对账 |
| 质量门禁 | Phase 6/7/8 定向测试 + 前端流程 + `quality.ps1 -Scope full` |
| 停止门禁 | 用户确认运维信息密度和重试入口 |

### Phase UI-7：扩展搜索与选题建议

| 字段 | 内容 |
|---|---|
| 目标 | 完成临时搜索、确认归档、重复关联及选题生成闭环 |
| 覆盖 | F11、F12；AC-F24、F26；AC-I15；AC-E12 |
| 前置 | 手动选题 API 获批；扩展搜索继续使用 Fake/未配置状态 |
| 计划文件 | `features/intel-detail/expansion/*`、`topic-ideas/*`、contracts；必要时 API/tests |
| 任务 | 搜索中/结果/无结果/失败；临时标识；收藏确认；按返回值显示新建或关联；自动/手动选题的标题、大纲、爆点和支撑来源；生成失败重试 |
| 禁止 | 未收藏即入库；前端自行去重；未配置服务时伪造内容；生成完整文章或自动发布 |
| 测试 | 未收藏无正式档案、重复关联、加工门禁、失败保留原情报、选题生成状态 |
| 验收证据 | 扩展搜索两条成功分支与失败分支；选题来源可追溯 |
| 质量门禁 | Phase 7 定向测试 + 前端 E2E + `quality.ps1 -Scope full` |
| 停止门禁 | 用户确认结果卡、确认步骤和选题结构 |

### Phase UI-8：状态矩阵、无障碍、性能与最终验收

| 字段 | 内容 |
|---|---|
| 目标 | 对 PRD、Figma 和实现做最终逐项对账 |
| 覆盖 | 全部本文件范围内 FR/AC；重点 AC-D03、D13、I03～05、E13～16、M03～05 |
| 前置 | UI-1～7 均通过 |
| 计划文件 | 测试、E2E、必要样式修正、`docs/ui-upgrade/*`；不得扩展业务范围 |
| 任务 | 通用状态全覆盖；键盘/焦点/标签/对比度；1440×1000 与小屏降级；外部文本安全；1 万条性能；六条核心流程 E2E；追踪矩阵终审 |
| 禁止 | 为通过测试修改 PRD阈值、删除既有测试或放宽安全/性能标准 |
| 测试 | `quality.ps1 -Scope full`、Phase 8 安全/性能、浏览器六流程、手工视觉清单 |
| 验收证据 | 命令原始输出、截图索引、FR/AC 状态、已知风险与延期项 |
| 质量门禁 | 全量质量门禁必须退出码 0；任何未运行项标为 NOT_RUN |
| 停止门禁 | 提交最终验收报告，等待用户决定是否进入真实适配或试运行 |

## 8. 测试策略

### 8.1 前端测试

- 共享组件：状态、文案、可访问名称、禁用和确认。
- Feature：每页至少覆盖 loading、success、empty、partial/error 中适用状态。
- Mutation：pending 时防重复；成功后读取权威数据；失败保留输入并可重试。
- 契约 fixture：不得直接复制 Figma 演示对象；应构造满足 TypeScript/Pydantic 的最小数据。
- 不新增测试库；优先使用现有 Vitest、Testing Library、Playwright 环境。

### 8.2 后端契约测试

- 所有新增查询参数做边界和组合测试。
- 详情 DTO 校验来源、版本、观点和评分历史的一致性。
- 运行与评分接口验证确认门禁、幂等和旧记录保留。
- 若有迁移，必须验证空库到 head、已有库升级以及 schema 对账。

### 8.3 浏览器流程

1. 今日/专题/检索 → 详情 → 来源 → 版本 → 选题。
2. 详情 → 收藏/置顶/已读/笔记 → 刷新仍保留。
3. 详情 → 扩展搜索 → 新档案；另测重复关联和失败。
4. 质量反馈 → 校准建议 → 确认/拒绝 → 回退 → 重评分。
5. 来源新增/校验/暂停/恢复/逻辑删除 → 设置顺序和配置状态。
6. 运行失败 → 阶段详情 → 局部重试；飞书仅补失败分段；回收站恢复/永久删除。

### 8.4 质量命令

```powershell
powershell -File scripts/quality.ps1 -Scope frontend
.\.venv\Scripts\python.exe -B -m pytest tests\phase7 -q
.\.venv\Scripts\python.exe -B -m pytest tests\phase8 -q
powershell -File scripts/quality.ps1 -Scope full
```

只有真实执行且退出码为 0 才能写“通过”。第三方警告必须记录，不得隐去。

## 9. FR 追踪矩阵

| FR | UI Phase | 架构所有者 | 主要证据 |
|---|---|---|---|
| F1 信息源与规则管理 | UI-4 | sources feature + source API | CRUD、GitHub/视频状态、下次生效 |
| F2 夜间滚动采集 | UI-4、UI-6 | settings/runs + pipeline read model | 窗口、补跑、超时与锁状态 |
| F3 聚合与版本 | UI-3 | detail + archive detail DTO | 多来源、观点、版本时间线 |
| F4 双语加工 | UI-3 | detail viewer | 原文、译文、摘要、结论、PM价值 |
| F5 专题分类 | UI-1、UI-2、UI-4 | navigation/topics/settings | 四专题和二级标签 |
| F6 评分与分层 | UI-1、UI-2、UI-3、UI-4 | list/score/settings | 五维、阈值、排序、10/20/20 |
| F7 反馈与校准 | UI-5 | quality + calibration API | 确认、拒绝、版本、回退、重评分 |
| F8 本地只读归档 | UI-3 | detail/user metadata | 只读、笔记、元数据、回收站 |
| F9 飞书私聊 | UI-6 | runs/delivery read model | 分段状态和失败补发 UI |
| F10 PC 网页 | UI-1～8 | app/features/components | 8 个模块和状态矩阵 |
| F11 搜索 | UI-2、UI-7 | search/expansion | 本地融合检索和临时扩展结果 |
| F12 选题建议 | UI-3、UI-7 | topic ideas | 标题、大纲、爆点、支撑来源 |

## 10. AC 追踪矩阵（UI 专项）

本矩阵只覆盖 UI 升级直接实现或必须展示的 AC；其余采集、归档和真实适配验收继续由 v1.1-AC 管理。

| AC | Phase | 测试/可观察证据 |
|---|---|---|
| AC-F01 | UI-4 | 合法来源保存、字段回显、列表刷新 |
| AC-F02 | UI-4 | 专家启停/移除，历史数据仍可查看 |
| AC-F03 | UI-4 | GitHub 四项规则与项目白名单回显 |
| AC-F05 | UI-3 | 发布时间未知 + 首次发现时间 |
| AC-F06 | UI-3、UI-4 | 来源类型字段和原始链接 |
| AC-F07 | UI-4 | 无字幕时仅元数据提示 |
| AC-F08 | UI-3 | 英文原文、译文、摘要、结论、PM价值分区 |
| AC-F09 | UI-3 | 单事件聚合多个来源 |
| AC-F10 | UI-3 | 观点、来源和覆盖比例；无“置信度” |
| AC-F11 | UI-3 | 有更新与多版本时间线 |
| AC-F12 | UI-3 | 无变化不生成新版本的现有后端回归 |
| AC-F13 | UI-4 | 截断配置说明“仅新采集生效” |
| AC-F14 | UI-2 | 单主专题 + 多标签 |
| AC-F15 | UI-2 | 工程安全样例归类 |
| AC-F16 | UI-3 | 总分、五维、依据、配置版本 |
| AC-F17 | UI-2 | 正式列表无低于阈值内容；由后端保证 |
| AC-F18 | UI-2 | 超过 50 条排序/截断；前端不重算 |
| AC-F19 | UI-2 | 不足时显示真实数量 |
| AC-F20 | UI-2 | 必读/重要/扩展层级 |
| AC-F21 | UI-2 | “今日无达标情报”空日报 |
| AC-F22 | UI-3 | 详情 ID/版本/专题/评分/来源与后端对账 |
| AC-F23 | UI-6 | 推送层级/专题顺序和分段状态 |
| AC-F24 | UI-3、UI-7 | 自动/手动选题四类字段 |
| AC-F25 | UI-2 | 标题/摘要/正文/来源/标签/笔记命中 |
| AC-F26 | UI-7 | 未收藏不入库，收藏后去重 |
| AC-D01 | UI-2 | 状态、总数、三级、四专题入口 |
| AC-D02 | UI-2 | 四类图表 |
| AC-D03 | UI-2、UI-8 | 空态/不完整，不用 0 冒充 |
| AC-D04 | UI-1、UI-2 | 三层标识和优先顺序 |
| AC-D05 | UI-2 | 专题数量、标签和列表 |
| AC-D06 | UI-3 | 详情全部信息区 |
| AC-D07 | UI-3 | 双语分区 |
| AC-D08 | UI-3 | 来源名/链接/时间/观点/覆盖比例 |
| AC-D09 | UI-3 | 时间线、首次/最新切换 |
| AC-D10 | UI-3 | 来源不可访问但本地内容保留 |
| AC-D11 | UI-1、UI-3 | 系统只读与笔记视觉分离 |
| AC-D12 | UI-6 | 失败来源/阶段/时间/原因/次数/操作 |
| AC-D13 | UI-2、UI-8 | 1 万条冷启动 <5s、筛选 <2s |
| AC-D14 | UI-4 | 保存后网页顺序；推送顺序契约回归 |
| AC-I01 | UI-1、UI-3 | 无系统字段编辑入口；API 拒绝越权写 |
| AC-I02 | UI-3 | 笔记刷新/新版本后保留 |
| AC-I03 | UI-3、UI-6 | 后台运行时已完成档案可操作 |
| AC-I04 | UI-3、UI-6 | 处理中记录禁用详情/用户操作 |
| AC-I05 | UI-3 | 收藏/置顶/已读刷新保留 |
| AC-I06 | UI-3、UI-5 | 剔除原因必填并进入回收站 |
| AC-I07 | UI-3 | 恢复后专题/版本/笔记/元数据对账 |
| AC-I08 | UI-3 | 二次确认；取消不删除 |
| AC-I09 | UI-5 | 仅反馈时 active 配置不变 |
| AC-I10 | UI-5 | 样本量/建议/影响维度/范围 |
| AC-I11 | UI-5 | 确认生效、拒绝不变 |
| AC-I12 | UI-5 | 选择真实历史版本并记录回退 |
| AC-I13 | UI-5 | 重评分新增记录、旧分保留 |
| AC-I14 | UI-4 | 截断值下次采集生效 |
| AC-I15 | UI-7 | 重复扩展结果关联已有事件 |
| AC-E02 | UI-4、UI-6 | GitHub 排名失败但白名单继续 |
| AC-E05 | UI-6 | 仅失败阶段及依赖后续重试 |
| AC-E06 | UI-6 | 仅补发失败分段 |
| AC-E07 | UI-6 | 本地日报保留并有重推入口 |
| AC-E08 | UI-6 | 补跑状态可见 |
| AC-E09 | UI-6 | 重复启动被拒绝并留痕 |
| AC-E10 | UI-6 | 2 小时超时、已完成提交、未完成待补跑 |
| AC-E11 | UI-6 | 遗留锁恢复为待重试 |
| AC-E12 | UI-7 | 原情报不变、失败原因与重试 |
| AC-E13 | UI-4 | 只显示缺失项名称，不显示秘密 |
| AC-E14 | UI-3、UI-8 | 恶意 HTML 不执行 |
| AC-E15 | UI-8 | 外部指令只作文本 |
| AC-E16 | UI-6、UI-8 | 页面和日志均不泄露认证信息 |
| AC-M01 | UI-5、UI-8 | UI 写操作对应本地事件 |
| AC-M02 | UI-6 | run_id 可串联完整链路 |
| AC-M03 | UI-2、UI-6、UI-8 | 首页/运行/事件数据一致 |
| AC-M04 | UI-4、UI-6、UI-8 | UI/日志无明文秘密 |
| AC-M05 | UI-3、UI-8 | 永久删除后不再展示正文 |

## 11. 受保护行为

1. FastAPI 和 Vite 只监听 `127.0.0.1`。
2. SQLite、正式 Markdown、Raw 和 Vault View 的职责不变。
3. RawSnapshot、EventVersion、Evidence、Score 保持只追加。
4. 收藏、置顶、已读、笔记和回收站继续关联稳定 `event_id`。
5. 评分、分层、排序和覆盖比例由后端权威逻辑产生。
6. Embedding 未配置时显式降级；外部 provider 未配置时显式不可用。
7. 扩展结果未收藏不进入正式档案；收藏依赖正式加工门禁。
8. 现有 Phase 7 的 9 个浏览器写操作和 Phase 8 的真实 1 万条性能链路继续通过。
9. 不展示密钥，不执行外部内容中的脚本或操作指令。

## 12. 停止条件与禁止动作

出现以下任一情况，Codex 必须停止当前阶段并请求决定：

- Figma 与 PRD 的用户可见行为发生冲突；
- 详情形态、危险操作、状态反馈或评分编辑方式尚未确认；
- 需要新增依赖、数据库迁移、真实外部服务或凭证；
- API 无法提供 Figma 所需字段，且只能通过臆造数据继续；
- 需要改变四专题、阈值、上限、分层、只读或评分确认门禁；
- 工作区文件在执行期间发生无法归属的重叠变化；
- 质量基线出现新增失败、1 万条性能越界或安全扫描失败；
- 需要提交、建分支、推送、部署、创建计划任务或永久删除真实数据。

## 13. Codex 总控提示词

```markdown
你是 AI 产品经理情报知识库的 UI 升级实现工程师。

权威资料：
1. D:\AI_informatics\Ai情报搜集系统\AI情报知识库_PRD_工作稿.md（v3.0）
2. D:\AI_informatics\Ai情报搜集系统\AI情报知识库_Codex开发执行文档_v1.1.md（v1.1-AC）
3. D:\AI_informatics\Ai情报搜集系统\AI情报知识库_Codex_UI补全升级执行文档_v1.0.md（v1.0-UI）
4. D:\AI_informatics\Ai情报搜集系统\ai-intel-kb\README_FOR_AI.md
5. Figma PjXzkYaVIX4BxV26eW1Tk6，页面 19:2

权威顺序：
- 当前用户明确指令最高。
- PRD 定义做什么；UI 执行文档定义如何分阶段实施。
- Figma 定义布局、信息层级和状态参考，但演示数据不构成业务事实。
- 现有代码定义必须保护的行为；历史报告不能覆盖 PRD。

当前阶段：
- 只执行用户明确授权的一个 UI Phase。
- 编辑前报告 Phase、FR/AC、拟改文件、测试和冲突。
- 不实现后续 Phase。
- 运行阶段真实质量门禁，提交阶段报告后停止。
- 未经用户确认不得进入下一阶段。

工程边界：
- 不修改 PRD，不发明业务状态、阈值、权限或验收标准。
- 不把 121 个原型状态做成 121 个路由。
- 业务规则留在 domain/application/repository；UI 只展示权威结果。
- 不硬编码 Figma 演示数据，不使用 dangerouslySetInnerHTML。
- 不新增依赖、迁移、真实集成、凭证或破坏性重置，除非本阶段已明确授权。
- 保留用户改动；项目整体未被 Git 跟踪，编辑前后需逐文件核对。
- 不提交、建分支、推送、部署或发送真实消息。

项目红线：
- 仅监听本机。
- 系统档案只读；用户笔记与元数据独立。
- 四专题、70 分、50 条和 10/20/20 不得改变。
- 评分调整必须用户确认并版本化；旧评分保留。
- 扩展结果未收藏不入库；去重由后端完成。
- 外部服务未授权时使用 Fake 或明确不可用状态。
- 任何密钥、Token、Cookie 和认证头不得出现在 UI、日志、fixture 或文档。

阶段报告：
1. 当前 Phase 与结果。
2. 覆盖的 FR/AC。
3. 新增、修改和明确未动的文件。
4. 依据 PRD 作出的决定。
5. 测试命令与真实结果。
6. 未完成项。
7. 风险和需要用户决定的事项。
8. 仅给出下一阶段建议，然后停止。
```

## 14. 单阶段提示词模板

```markdown
使用以下权威资料：
- AI情报知识库_PRD_工作稿.md v3.0
- AI情报知识库_Codex开发执行文档_v1.1.md v1.1-AC
- AI情报知识库_Codex_UI补全升级执行文档_v1.0.md v1.0-UI
- ai-intel-kb/README_FOR_AI.md
- Figma PjXzkYaVIX4BxV26eW1Tk6 page 19:2

只执行 Phase UI-<N>：<阶段名称>。

编辑前先报告覆盖 FR/AC、计划文件、验证命令和冲突。
不得修改 PRD、扩大范围、引入未授权依赖/迁移/真实集成或开始下一阶段。
完成后运行该阶段真实质量门禁，报告真实结果并停止。
```

## 15. 阶段交付与最终清单

每阶段至少交付：

- 源码及同阶段测试；
- API/DTO 变更说明；
- 截图或可观察 UI 证据；
- FR/AC 覆盖状态；
- 真实质量命令输出摘要；
- 未实现范围、风险和决策记录。

UI-8 最终交付：

- 8 个主模块和详情工作区；
- 通用组件与设计 token；
- 完整加载/空/部分/失败/重试/禁用/成功/确认状态；
- 6 条浏览器业务流程；
- 1 万条性能证据；
- 安全与凭证脱敏证据；
- UI 追踪矩阵最终报告；
- 明确列出的延期项和真实适配未授权项。

## 16. 当前执行起点

本文件生成后不得直接开始改代码。下一次实现任务必须从 **Phase UI-0** 开始，先确认 UI-DEC-01～04、刷新工作区状态和质量基线，然后停止等待用户批准 Phase UI-1。
